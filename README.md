make sure you are in the / aka root directory
type: git clone https://github.com/embermander/mk
function mk { bash /mk.sh;}
note: Whenever you reboot or reset the terminal or OS then type the second command
